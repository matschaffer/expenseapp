<%= form_for(@purchase, html: { class: 'nice' }) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, class: 'input-text' %>
  </div>
  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount, class: 'input-text' %>
  </div>
  <div class="field">
    <%= f.label :tag_list %>
    <%= f.text_field :tag_list, class: 'input-text' %>
  </div>

  <div class="actions">
    <%= f.submit class: std_button %>
  </div>
<% end %>
